# 이 저장소에서 지킬 것

## 커밋

- **커밋 메시지에 Claude·Anthropic 을 넣지 않는다.**
  `Co-Authored-By: Claude`, `Claude-Session:`, `Generated with Claude Code`
  같은 줄을 어떤 형태로도 붙이지 않는다. PR 본문도 같다.
- **작성자는 항상 저장소 주인이다.** 커밋 전에 확인한다.

  ```
  git config user.name kinggodyoung
  git config user.email woow529@goedu.kr
  ```

  환경에 따라 전역 git 설정이 `Claude <noreply@anthropic.com>` 로
  덮여 있을 수 있다. 저장소별 설정이 전역보다 우선하므로 위 두 줄을
  먼저 실행한 뒤 커밋한다.
- 커밋 직후 `git log -1 --format="%an <%ae>%n%B"` 로 작성자와 메시지를
  눈으로 확인한다.

## 글쓰기

- **줄바꿈은 단어가 끝난 뒤에 한다.** 단어 중간에서 줄이 끊기지 않게
  한 줄을 적당히 짧게 유지한다. 채팅 답변, 커밋 메시지, 문서, 코드 주석
  전부 해당한다.
- 표의 칸 내용은 짧게 쓴다. 길면 표 밖으로 뺀다.
- 한국어로 쓴다. 번역투를 피한다.
  - `~에 대해` → `~을`, `~를 통해` → `~으로`, `~되어진다` → `~된다`
  - `가지고 있다` → `있다`, `~중 하나이다` → `~이다`
- 어려운 말은 풀어 쓴다. 처음 나오는 용어는 그 자리에서 설명한다.
  - 백테스트 → 매매 전략을 과거에 돌려본 검증
  - 슬리피지 → 원하는 가격에 못 사고 밀리는 손실
  - 파라미터 → 설정값

## 이 앱의 코드에서 특히 조심할 것

- **판정에 뒷날 봉이 필요한 규칙은 성과도 그날부터 재야 한다.**
  `for (let k = i; k <= i + N; ...)` 처럼 앞날을 보는 조건을 넣었다면
  성과는 `i` 가 아니라 `i + N` 부터 잰다. `outcome.js` 의
  `confirmedOutcome()` 을 쓰면 된다. 안 그러면 승률이 조용히 부풀려진다.
- **하락 신호는 상승 비율이 낮아야 맞힌 것이다.**
  `winRate - baseline` 을 그대로 "신호가 잘 맞았다"로 쓰지 않는다.
  `stats.js` 의 `directionalEdge()` 를 쓴다.
- 통계 문구를 쓸 때 "대부분의 신호는 의미 없다" 처럼 넓게 말하지 않는다.
  이 앱이 재는 것은 진입 시점 하나뿐이고, 손절·익절·포지션 크기는
  들어 있지 않다. "신호 하나만 보고 기계적으로 사고파는 방식으로는"
  까지 붙여서 좁게 쓴다.
- 수치를 문서에 적었으면 데이터를 다시 만든 뒤 그 수치도 같이 고친다.
  README, DEVELOPMENT.md, 레슨 본문에 숫자가 흩어져 있다.
